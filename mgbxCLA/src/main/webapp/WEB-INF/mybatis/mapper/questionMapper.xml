<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="question">

	<insert id="insertQuestion1" parameterType="com.sp.customer.question.Question">
		INSERT INTO question (code, subject, content, isAnswer, userId) 
		VALUES (QUESTION_seq.NEXTVAL, #{subject}, #{content} , #{isAnswer} , #{userId})
	</insert>
	
	<insert id="insertQuestion2" parameterType="com.sp.customer.question.Question">
		INSERT INTO questionAnswer (code, subject, content, , userName, userId) 
		VALUES (#{code} , #{subject}, #{content} ,#{userName}, #{userId})
	</insert>
	
	<insert id="insertQuestion3" parameterType="com.sp.customer.question.Question">
		INSERT INTO questionCate (cateCode, cateName) 
		VALUES (questionCate_seq.NEXTVAL , #{cateName})
	</insert>

</mapper>